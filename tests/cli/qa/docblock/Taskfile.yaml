version: "3"

tasks:
  test:
    cmds:
      - |
          bash <<'EOF'
          set -euo pipefail
          source ../../_lib/run.sh

          go build -trimpath -ldflags="-s -w" -o bin/core ../_harness

          output="$(mktemp)"
          run_capture_stdout 1 "$output" ./bin/core qa docblock --json --threshold 100 fixtures/src
          jq -e '(.passed == false) and (.coverage < .threshold)' "$output" >/dev/null
          jq -e '(.missing | length == 2) and (.missing[0].name == "Beta") and (.missing[1].name == "Broken")' "$output" >/dev/null
          jq -e '(.warnings | length == 0)' "$output" >/dev/null
          EOF
