#!/usr/bin/env sh

junit=""

while [ $# -gt 0 ]; do
  case "$1" in
    --log-junit)
      shift
      junit="$1"
      ;;
  esac
  shift
done

printf '%s' '<testsuite tests="1" failures="1"></testsuite>' >"$junit"
